# set your user tokens as environment variables, such as ~/.secrets
# See the README for examples.
[color]
  ui = true
[color "branch"]
  current = yellow reverse
  local = yellow
  remote = green
[color "diff"]
  meta = yellow bold
  frag = magenta bold
  old = red
  new = green
[include]
  path = .gitconfig.user
[merge]
	log = false
[core]
	excludesfile = /nail/home/mluck/.gitignore_global
[apply]
	whitespace = nowarn
